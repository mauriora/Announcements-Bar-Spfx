{
    "name": "@mauriora/announcements-bar-spfx",
    "version": "0.1.1",
    "repository": {
        "url": "https://github.com/mauriora/Announcements-Bar-Spfx.git"
    },
    "private": true,
    "workspaces": [
        "shared/**",
        "app/**"
    ],
    "scripts": {
        "clean-node-modules": "rimraf ./shared/*/node_modules && rimraf app/*/node_modules && rimraf yarn.lock && rimraf ./node_modules",
        "clean-node-modules-az": "rimraf yarn.lock && yarn clean-node-modules"
    },
    "devDependencies": {
        "rimraf": "^3.0.2",
        "yarn": "^1.0.0"
    },
    "dependencies": {}
}
